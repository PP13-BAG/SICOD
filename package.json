{
  "name": "sitrep-cod",
  "version": "1.0.0",
  "description": "Générateur de Points de Situation — COD Préfecture 13",
  "main": "backend/server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js",
    "cf:dev": "npx wrangler pages dev frontend",
    "cf:deploy": "npx wrangler pages deploy frontend --project-name sicod-pages --branch production --commit-dirty=true",
    "cf:d1:migrate": "npx wrangler d1 migrations apply sicod-prod --remote --config wrangler.jsonc",
    "cf:d1:migrate:local": "npx wrangler d1 migrations apply sicod-preview --local --config wrangler.jsonc",
    "cf:d1:migrate:preview": "npx wrangler d1 migrations apply sicod-preview --remote --preview --config wrangler.jsonc"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "sql.js": "^1.14.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "wrangler": "^4.41.0"
  }
}
